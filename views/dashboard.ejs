<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title%></title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bulma@1.0.0/css/bulma.min.css"
    />
</head>
<body>
    <%- include('navbar') -%>
    
    <div class = 'container'>
      <h1>This is admin page</h1>
      <div class = 'column'>
        <table class="table">
          <thead>
            <tr>
              <th>Username</th>
              <th>Password</th>
            </tr>
          </thead>
          <tbody>
            <% userData.forEach(user => { %>
            <tr>
              <td><%= user.username%></td>
              <td><%= user.password%></td>
              <td>
                <button><a class="button is-warning" href="#">Edit</a></button>
                <button><a class="button is-warning" href="#">Delete</a></button>
              </td>
            </tr>
            <% }); %>
          </tbody>
        </table>
      </div>
    </div>
    <a href="/users/logout" class="btn btn-sm btn-label-danger">Log out</a>
</body>
</html>